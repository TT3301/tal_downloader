diff --git a/.github/workflows/build.yml b/.github/workflows/build.yml
index dd7f8de..26301ae 100644
--- a/.github/workflows/build.yml
+++ b/.github/workflows/build.yml
@@ -3,6 +3,12 @@ name: Build
 on:
   release:
     types: [published]
+  workflow_dispatch:
+    inputs:
+      version:
+        description: "Release tag to build, for example v3.2.4"
+        required: true
+        type: string
 
 permissions:
   contents: write
@@ -35,7 +41,7 @@ jobs:
 
       - name: Update version info
         run: |
-          TAG_NAME="${{ github.event.release.tag_name }}"
+          TAG_NAME="${{ github.event.release.tag_name || inputs.version }}"
           VERSION="${TAG_NAME#v}"  # 移除v前缀
           BUILD_TIME=$(date -u '+%Y-%m-%d %H:%M:%S UTC')
           
@@ -53,7 +59,7 @@ jobs:
           GOOS: linux
           GOARCH: amd64
         run: |
-          TAG_NAME="${{ github.event.release.tag_name }}"
+          TAG_NAME="${{ github.event.release.tag_name || inputs.version }}"
           VERSION="${TAG_NAME#v}"  # 移除v前缀
           APPNAME=temp_linux_build
           FINAL_FILENAME=tal_downloader_linux_amd64_$TAG_NAME
@@ -64,17 +70,17 @@ jobs:
       - name: Release Linux
         uses: svenstaro/upload-release-action@v2
         with:
-          tag: ${{ github.event.release.tag_name }}
-          release_name: ${{ github.event.release.tag_name }}
+          tag: ${{ github.event.release.tag_name || inputs.version }}
+          release_name: ${{ github.event.release.tag_name || inputs.version }}
           overwrite: true
-          file: tal_downloader_linux_amd64_${{ github.event.release.tag_name }}
+          file: tal_downloader_linux_amd64_${{ github.event.release.tag_name || inputs.version }}
 
       - name: Upload Artifacts
         uses: actions/upload-artifact@v4
         with:
           name: "Linux-build"
           path: |
-            tal_downloader_linux_amd64_${{ github.event.release.tag_name }}
+            tal_downloader_linux_amd64_${{ github.event.release.tag_name || inputs.version }}
 
   Build_Windows:
     runs-on: windows-latest
@@ -92,7 +98,7 @@ jobs:
       - name: Update version info
         shell: bash
         run: |
-          TAG_NAME="${{ github.event.release.tag_name }}"
+          TAG_NAME="${{ github.event.release.tag_name || inputs.version }}"
           VERSION="${TAG_NAME#v}"  # 移除v前缀
           BUILD_TIME=$(date -u '+%Y-%m-%d %H:%M:%S UTC')
           
@@ -111,7 +117,7 @@ jobs:
           GOARCH: amd64
         shell: bash
         run: |
-          TAG_NAME="${{ github.event.release.tag_name }}"
+          TAG_NAME="${{ github.event.release.tag_name || inputs.version }}"
           VERSION="${TAG_NAME#v}"  # 移除v前缀
           APPNAME="好未来课程下载器"
           FINAL_FILENAME=tal_downloader_win64_$TAG_NAME.exe
@@ -134,17 +140,17 @@ jobs:
       - name: Release Windows
         uses: svenstaro/upload-release-action@v2
         with:
-          tag: ${{ github.event.release.tag_name }}
-          release_name: ${{ github.event.release.tag_name }}
+          tag: ${{ github.event.release.tag_name || inputs.version }}
+          release_name: ${{ github.event.release.tag_name || inputs.version }}
           overwrite: true
-          file: tal_downloader_win64_${{ github.event.release.tag_name }}.exe
+          file: tal_downloader_win64_${{ github.event.release.tag_name || inputs.version }}.exe
 
       - name: Upload Artifacts
         uses: actions/upload-artifact@v4
         with:
           name: "Windows-build"
           path: |
-            tal_downloader_win64_${{ github.event.release.tag_name }}.exe
+            tal_downloader_win64_${{ github.event.release.tag_name || inputs.version }}.exe
 
   Build_MacOS_ARM64_Android:
     runs-on: macos-latest
@@ -166,7 +172,7 @@ jobs:
 
       - name: Update version info
         run: |
-          TAG_NAME="${{ github.event.release.tag_name }}"
+          TAG_NAME="${{ github.event.release.tag_name || inputs.version }}"
           VERSION="${TAG_NAME#v}"  # 移除v前缀
           BUILD_TIME=$(date -u '+%Y-%m-%d %H:%M:%S UTC')
           
@@ -182,7 +188,7 @@ jobs:
         env:
           CGO_ENABLED: 1
         run: |
-          TAG_NAME="${{ github.event.release.tag_name }}"
+          TAG_NAME="${{ github.event.release.tag_name || inputs.version }}"
           VERSION="${TAG_NAME#v}"  # 移除v前缀
           APPNAME="好未来课程下载器"
           FINAL_FILENAME=tal_downloader_macos_arm64_$TAG_NAME
@@ -206,7 +212,7 @@ jobs:
         env:
           CGO_ENABLED: 1
         run: |
-          TAG_NAME="${{ github.event.release.tag_name }}"
+          TAG_NAME="${{ github.event.release.tag_name || inputs.version }}"
           VERSION="${TAG_NAME#v}"  # 移除v前缀
           APPNAME="好未来课程下载器"
           FILENAME="go________"  # 特殊命名处理fyne的中文bug
@@ -230,7 +236,7 @@ jobs:
         env:
           CGO_ENABLED: 1
         run: |
-          TAG_NAME="${{ github.event.release.tag_name }}"
+          TAG_NAME="${{ github.event.release.tag_name || inputs.version }}"
           VERSION="${TAG_NAME#v}"  # 移除v前缀
           APPNAME="好未来课程下载器"
           FILENAME="go________"  # 特殊命名处理fyne的中文bug
@@ -253,35 +259,35 @@ jobs:
       - name: Release macOS ARM64
         uses: svenstaro/upload-release-action@v2
         with:
-          tag: ${{ github.event.release.tag_name }}
-          release_name: ${{ github.event.release.tag_name }}
+          tag: ${{ github.event.release.tag_name || inputs.version }}
+          release_name: ${{ github.event.release.tag_name || inputs.version }}
           overwrite: true
-          file: tal_downloader_macos_arm64_${{ github.event.release.tag_name }}.zip
+          file: tal_downloader_macos_arm64_${{ github.event.release.tag_name || inputs.version }}.zip
 
       - name: Release Android ARM64
         uses: svenstaro/upload-release-action@v2
         with:
-          tag: ${{ github.event.release.tag_name }}
-          release_name: ${{ github.event.release.tag_name }}
+          tag: ${{ github.event.release.tag_name || inputs.version }}
+          release_name: ${{ github.event.release.tag_name || inputs.version }}
           overwrite: true
-          file: tal_downloader_android_arm64_${{ github.event.release.tag_name }}.apk
+          file: tal_downloader_android_arm64_${{ github.event.release.tag_name || inputs.version }}.apk
 
       - name: Release Android ARM
         uses: svenstaro/upload-release-action@v2
         with:
-          tag: ${{ github.event.release.tag_name }}
-          release_name: ${{ github.event.release.tag_name }}
+          tag: ${{ github.event.release.tag_name || inputs.version }}
+          release_name: ${{ github.event.release.tag_name || inputs.version }}
           overwrite: true
-          file: tal_downloader_android_arm_${{ github.event.release.tag_name }}.apk
+          file: tal_downloader_android_arm_${{ github.event.release.tag_name || inputs.version }}.apk
 
       - name: Upload Artifacts
         uses: actions/upload-artifact@v4
         with:
           name: "MacOS-ARM64-Android-build"
           path: |
-            tal_downloader_macos_arm64_${{ github.event.release.tag_name }}.zip
-            tal_downloader_android_arm64_${{ github.event.release.tag_name }}.apk
-            tal_downloader_android_arm_${{ github.event.release.tag_name }}.apk
+            tal_downloader_macos_arm64_${{ github.event.release.tag_name || inputs.version }}.zip
+            tal_downloader_android_arm64_${{ github.event.release.tag_name || inputs.version }}.apk
+            tal_downloader_android_arm_${{ github.event.release.tag_name || inputs.version }}.apk
 
   Build_MacOS_Intel:
     runs-on: macos-13
@@ -298,7 +304,7 @@ jobs:
 
       - name: Update version info
         run: |
-          TAG_NAME="${{ github.event.release.tag_name }}"
+          TAG_NAME="${{ github.event.release.tag_name || inputs.version }}"
           VERSION="${TAG_NAME#v}"  # 移除v前缀
           BUILD_TIME=$(date -u '+%Y-%m-%d %H:%M:%S UTC')
           
@@ -314,7 +320,7 @@ jobs:
         env:
           CGO_ENABLED: 1
         run: |
-          TAG_NAME="${{ github.event.release.tag_name }}"
+          TAG_NAME="${{ github.event.release.tag_name || inputs.version }}"
           VERSION="${TAG_NAME#v}"  # 移除v前缀
           APPNAME="好未来课程下载器"
           FINAL_FILENAME=tal_downloader_macos_x86_64_$TAG_NAME
@@ -337,13 +343,13 @@ jobs:
       - name: Release macOS x86_64
         uses: svenstaro/upload-release-action@v2
         with:
-          tag: ${{ github.event.release.tag_name }}
-          release_name: ${{ github.event.release.tag_name }}
+          tag: ${{ github.event.release.tag_name || inputs.version }}
+          release_name: ${{ github.event.release.tag_name || inputs.version }}
           overwrite: true
-          file: tal_downloader_macos_x86_64_${{ github.event.release.tag_name }}.zip
+          file: tal_downloader_macos_x86_64_${{ github.event.release.tag_name || inputs.version }}.zip
 
       - name: Upload Artifacts
         uses: actions/upload-artifact@v4
         with:
           name: "MacOS-x86_64-build"
-          path: tal_downloader_macos_x86_64_${{ github.event.release.tag_name }}.zip
+          path: tal_downloader_macos_x86_64_${{ github.event.release.tag_name || inputs.version }}.zip
